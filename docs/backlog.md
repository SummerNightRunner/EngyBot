# Backlog

## Завершено

### База проекта

- стек выбран и зафиксирован;
- Docker и PostgreSQL настроены;
- доступен локальный и контейнерный запуск;
- базовая структура проекта собрана.

### Профиль пользователя

- реализован онбординг;
- сохраняются родной язык, изучаемый язык и уровень;
- в профиль добавлен флаг bilingual UI.

### Контент

- добавлен seed-файл словаря;
- добавлен seed-файл grammar units;
- добавлен seed-файл course units;
- добавлен seed-файл dialogues;
- course units связывают vocabulary, grammar и dialogues;
- к каждому seed-диалогу добавлено gap-fill задание;
- контент фильтруется по уровню;
- словарные темы масштабируются через несколько CEFR-уровней.

### Practice

- реализовано несколько форматов квизов;
- quiz sessions сокращены;
- добавлен immediate feedback;
- daily practice хранится по дате;
- добавлен review на основе ошибок.
- добавлено интервальное повторение по `mastery_level` и `review_due_at`.

### Progress

- сохраняется история попыток;
- сохраняется progress по отдельным словам;
- доступны weak words и topic progress;
- доступен счетчик слов, которые пора повторить;
- доступна сводка по цели дня.

## В работе

### Выравнивание bilingual UI

Bilingual pattern уже реализован, но еще не применяется последовательно на всех экранах.

### Unit progress

Course units уже используются в навигации, но прогресс по отдельным units пока не хранится.

### Grammar practice

Grammar units уже есть как контентный слой. Следующий шаг — отдельные упражнения по грамматике.

## Дальше

1. связать practice и review с units;
2. добавить прогресс по units;
3. добавить более развитую grammar practice;
4. завершить выравнивание bilingual UI;
5. расширить dialogue coverage для B2-C2;
6. добавить listening-like и speaking-like режимы.

## Пока вне текущего объема

- отдельная admin panel;
- микросервисное разделение;
- преждевременное усложнение миграций;
- несвязанные AI-функции.
